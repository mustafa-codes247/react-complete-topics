import React from 'react'

// funciton is made with 
const Functionsyntax = () => {
  return (
    <div>
        {/* this is basic syntax of function */}
        {/* xml is written inside these fucntions , its similar to html but its not */}
        {/* always start func name with capital letter */}

        <h1 style={{fontSize:"bold"}}>this is basic function</h1>
      
    </div>
  )
}

// this is export default where only one function per conponent can be made/called 
// if we want to make/call multiple function from same components , we use custom export/import
export default Functionsyntax
